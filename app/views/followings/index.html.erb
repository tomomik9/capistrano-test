<h1 class="users"><%= @user.username %></h1>
<h2 class="users"><%= t('.followed') %></h2>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to "#{user.username}", user_path(user) %>
     </li>
  <% end %>
</ul>
</br>
<%= link_to t('users.index.Users'), users_path %>
